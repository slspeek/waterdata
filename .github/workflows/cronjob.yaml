name: Cronjob

on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron:  '*/10 * * * *'

jobs:

  testjob:
    runs-on: ubuntu-latest
    steps:
      - name: Message
        run: echo Test job